# ❄️ E1 동결 선언 (FREEZE) — 2026-07-05

> 05 문서 §6에 따라 E1 착수 시점의 실험 요소를 커밋 해시로 동결한다.
> **이후 아래 요소를 변경하면 해당 실험 전체 재실행 + 본 파일에 개정 기록 (04 §8).**

## 동결 커밋

**`539b18d6e7a37597bf3d7d891f653d5bda89f769`** (paper 리모트 main, 2026-07-05)

## 동결 요소

| 요소 | 내용 | 파일 |
|---|---|---|
| Detector 모델 | **gemini-2.5-flash** (api 백엔드, temperature=0, thinking_budget=0, JSON 강제) | `agents/stage2b_claude_agent.py` |
| Validator 모델 | **gpt-4o** (temperature=0, 표본 상한 80건 결정론 샘플링) | `agents/stage3a_openai_judge.py` |
| 2B 프롬프트 | v3 슬림 (few-shot k=3, 에코 제거) | `prompts/stage2b_system_v3_slim.txt` |
| 배치 크기 | 20행/호출 (A2·A3 동일 — 공정성 프로토콜) | 상동 |
| 모호성 판정 | 중앙값/MAD 수정 Z > 3.5 + 교차필드 날짜쌍 | `agents/stage2a_deterministic.py` |
| DQ 점수 산식 | ISO 25012 5차원, config 무관 동일 | `config/dq_scoring.json` |
| 비용 산정 | 공시 단가 환산 (rate_date 2026-07-05) | `config/pricing.json` |
| 합의 로직 | 3소스 합의 (기존 Stage 4) | `agents/stage4_report_agent.py` |
| F1 예측 정의 | changelog 전체 개입 (normalize/fill/flag) | `run_experiments.py` `_evaluate` |
| 오류 주입 | T1~T5, 셀 기준 ρ, 유형 균등, 시드 고정 | `tools/inject_errors.py` |

## 비동결 (자유 변수)

병렬 워커 수(DQ_MAX_WORKERS), 레이트리밋 재시도 대기 — wall-clock에만 영향, 판정·비용 불변.

## E1 실행 규정 (04 문서 준수 확인)

- 데이터: S1 주입 배치 10종 (ρ{10,20}% × 시드{1..5}) — 생성기·시드 동결로 재현 가능
- 구성: A1~A5 × 10배치 = 50회, 백엔드 api 고정
- 실패: run 단위 1회 재시도 후 결측(failed/partial) 기록, silent retry 금지
- 기록: bench-result 회차 자동 기록 + 자동 커밋
